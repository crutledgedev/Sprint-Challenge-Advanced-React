import React from 'react';
import ReactDOM from 'react-dom';
import { render } from '@testing-library/react';
import PlayerCard from './PlayerCard';

test('rennders without crashing', () => {
    render(<PlayerCard />);
    // const { getByCard } = render(<PlayerCard />);
    // getByCard(/player/i); 

})